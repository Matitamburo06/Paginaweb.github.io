//CONSTRUCTORESSS


function msjAlert(contenido,posicion){
    this.texto=contenido;
    this.posicion=ubicacion;
}

var etiqueta = document.createElement("span");
var mensaje = document.createTextNode(`El dato" "${texto}"  "no es valido`);





etiqueta.appendChild(mensaje);

document.getElementsById(posicion).appendChild(etiqueta)


const alertamail = new msjAlert(getElementById());